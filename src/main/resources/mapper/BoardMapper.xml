<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.koreait.spring.board.BoardMapper">

    <insert id="insBoard" useGeneratedKeys="true" keyProperty="iboard">
        INSERT INTO t_board(title,ctnt,iuser) VALUES(#{title},#{ctnt},#{iuser})
    </insert>

    <select id="selBoardList" resultType="BoardDomain">
        <!--select만 꼭 resultType 적어야 함-->
        SELECT A.title, A.iboard, B.iuser, A.regdt
        , B.unm AS writerNm , B.profileImg
        FROM t_board A
        INNER JOIN t_user B
        ON A.iuser = B. iuser
        ORDER BY A.iboard DESC;
    </select>

    <select id="selBoard" resultType="BoardDomain">
        SELECT A.title, A.iboard, A.ctnt, B.iuser, A.regdt
             , B.unm AS writerNm , B.profileImg
        FROM t_board A
        INNER JOIN t_user B
        ON A.iuser = B. iuser
        WHERE A.iboard = #{iboard};
<!--#이 붙었으면 parameter가 있다는 말-->
    </select>

    <select id="delBoard">
        DELETE FROM t_board WHERE iboard=? AND iuser=?
    </select>
</mapper>
